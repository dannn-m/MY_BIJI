# stp的工作流程
- 选举根桥
- 选举根端口
- 选举指定端口
- 其他端口禁用
# stp状态
- disable
- blocking
- listening
- learning
- forwording
# 转发概念
- 次优的bpdu会被直接丢弃
- 交换机向下发送bpdu时会继承自身收到的最优的根桥id并将自身收到该报文的接口方向的开销值累加到报文中进行转发
选举根桥：
- bid
交换机开机先进入listening，并泛洪rid为自身的报文，最优的为根

选举根端口：
-  开销
- 网桥id
- 端口id
在自身的接口中比均是收到最优报文的接口被举出

选举指定端口：
- 根桥id
- 开销
- 网桥id
- 端口id
将该接口自身准备发往其他网段的报文与该接口收到的报文进行对比，自身的优则为指定（一个网段有一个指定）

注意：实际上这些报文在listening阶段被发出后就在同步被计算选举，因为stp会自动丢弃不是最优的报文所以该系统能够自动收敛

错误更新：
自身端口挂掉他会把自己的禁用端口打开开始发送bpdu报文