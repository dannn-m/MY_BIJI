# 存储技术
## 结构：
==寄存器、高速缓冲存储器、主存储器和外存储器==
高速缓冲：cache
主存储：内存
外存：硬盘
![[Screenshot_2026-09-22-16-17-54-98_769977972775e0c6b41aa3dfaf766445.jpg]]
## 存储介质的选择：
![[Screenshot_2026-09-22-16-20-51-45_769977972775e0c6b41aa3dfaf766445.jpg]]
HDD 机械硬盘与 SSD 固态硬盘：
- 机械硬盘：
	- 便宜慢
	- 连续读取不错，随机不行
	- 恢复数据方便
	- 接口协议：IDE、SATA、SAS、SCSI、FC
- 固态：
	- 贵，快
	- 几乎不可恢复
	- 接口协议：M.2、U.2、SATA、mSATA、SAS、PCIE，家用 M.2 企业 U.2
	- 闪存是硬盘
## 常见储存的用途：
NVRAM：非易失存储器，用来报春启动配置文件
Flash：闪存用来保存华为/思科 IOS 系统软件；
ROM：只读，储存引导程序
RAM：即内存，保存当前运行的配置程序
# RAID(独立磁盘冗余阵列)
- 将多个单独的物理硬盘以不同方式组合成一个逻辑硬盘，提高==硬盘的读写性能和数据安全==
![[Screenshot_2026-09-22-16-53-26-89_769977972775e0c6b41aa3dfaf766445.jpg]]
==分块==:将一个分区分成多个大小相等的、地址相邻的块
==条带深度==:由一个或者多个分块构成
==条带==:多个磁盘上相同位置（编号）的分块
## RAID 的热备和重构
- 热备：当冗余的 RAID 组中某个硬盘失效时，在不影响当前系统的情况下，用备用硬盘自动顶替失效硬盘
- 分类：
	- 全局式：备用硬盘位系统中**所有的冗余 RAID 组共享**
	- 专用式：备用硬盘为系统中**某一组冗余 RAID 组专用**
## 基本概念
逻辑卷：按照不同的容量创建逻辑卷，通过 LUN（logic unit number）来标识（类似于分盘 c 盘 d 盘）
## 常用技术
RAID 0 和 ARID 1
![[Screenshot_2026-09-22-17-09-28-48_769977972775e0c6b41aa3dfaf766445.jpg]]
RAID 3 和 RAID 5
RAID 3：
![[Screenshot_2026-09-22-17-12-02-58_769977972775e0c6b41aa3dfaf766445.jpg]]
![[Screenshot_2026-09-22-17-13-37-79_769977972775e0c6b41aa3dfaf766445.jpg]]
![[Screenshot_2026-09-22-17-14-29-34_769977972775e0c6b41aa3dfaf766445.jpg]]
技术对比：

| RAID 级别 | RAID 0 | RAID 1 | RAID 5 | RAID 6 | RAID 10 |
| ---- | ---- | ---- | ---- | ---- | ---- |
| 可靠性 | 最低 | 高 | 较高 | 高 | 高 |
| 冗余类型 | 无 | 镜像冗余 | 校验冗余 | 校验冗余 | 镜像冗余 |
| 空间利用率 | 100% | 50% | (N-1)/N | (N-2)/N | 50% |
| 性能 | 最高 | 最低 | 较高 | 较高 | 高 |
| 允许坏盘数量 | 0 | N/2 | 1 | 2 | N/2 |
> 备注：有几个校验盘就运行换几个盘
# RAID 2.0

| 传统RAID | RAID2.0+ |
| ---- | ---- |
| 需要手动配置单独的全局或局部热备磁盘 | 分布式的热备空间，无需单独配置 |
| 多对一的重构，重构数据流串行写入单一的热备磁盘 | 多对多的重构，重构数据流并行写入多块磁盘 |
| 存在热点，重构时间长 | 负载均衡，重构时间短 |

---
# 网络存储体系
DAS、NAS、SAN
DAS：相当于移动硬盘性质
SAN：将存储接入网络，服务器等的储存数据通通过网络存储（甚至操作系统也可以安装在上面）
NAS:相当于百度云盘，只能储存文件数据
## SAN 
- FCSAN
	背景 ：解决 DAS 的拓展差，可以同时连接上百台服务器
	连接方式：FC 光纤，使用专用的 fc 交换机
	访问方式：可划多个 LUN
	链路速率：2 gbps、4 gbps、8 gbps
- IPSAN
	- 解决 fcsan 的价格以及管理问题
	- 直接用以太交换机连接
	- 后端划分多个 LUN
	- 速率：1 gbps、10、40、100 gbps
## NAS
 - 背景：大量数据需要共享和交换
 - 通过 CIFS(window 系统)、NFS（Linux 系统）协议共享给前端主机，可对同一目录进行并发读写
---

# 分布式存储/超融合
- 分布式存储一般由三个部分组成：**客户端、源数据节点和数据存储节点**，源数据节点相当于目录，数据存储节点采用==多副本或纠删码技术==实现数据冗余，分布式有以下特点：
	- 高性能
	- 高可靠
	- 容量大
	- 成本低
---
# 数据备份策略
- 备份类份：
	- 完全：每天全备份，易于管理
	- 差量：每周一天备份，其余每天备份和全备份差异部分
	- 增量：每周一天备份，其余每天备份和上次备份差异部分