# 数据通信方式
- 按通信方式分：
	- 单工
	- 半双工
	- 双工
- 同步方式分：
	- 异步传输：把各个字符分开传输，在字符之间==插入同步信息==
		- 特点：实现简单，但是影响传输效率，导致速率不会太高
	- 同步传输：在发送数据之前，先发一串同步字符串 SYNC，接收方检测到 2 个以上的 sync 字符就确认已经进入同步状态，开始准备接收数据
		- 效率更高，在短距离高速数据传输中，大多采用同步传输方式