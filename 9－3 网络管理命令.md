# ipconfig

| 分类       | 选项          | 用途                       |
| -------- | ----------- | ------------------------ |
| ipconfig | 无           | 显示IP、掩码、网关等基础信息          |
| ipconfig | /all        | 显示IP、掩码、网关、DHCP和MAC等详细信息 |
| ipconfig | /renew      | 更新DHCP配置，重新获IP           |
| ipconfig | /release    | 释放DHCP获得的IP地址            |
| ipconfig | /flushdns   | 清除DNS缓存信息                |
| ipconfig | /displaydns | 显示DNS缓存信息                |
# 故障诊断 ping traceroute
| 分类 | 选项 | 用途 | 备注 |
| ---- | ---- | ---- | ---- |
| ping | -t | 持续 ping，直到 Ctrl+C 中断 | 考查频率高 |
| ping | -a | Windows: 将 IP 解析为主机名<br>华为设备: 指定 ping 的源 IP 地址 | 考查频率高 |
| ping | -n Count | 设置 ping 包的个数 | 考查频率高 |
| traceroute / tracert | 无 | 跟踪网络传输路径<br>原理：递增 TTL 字段的 ICMP 回送错误报文 | |
| pathping | 无 | 结合了 ping 和 tracert 功能，可以显示通信线路上每个子网的延时和丢包率 | 了解即可 |

# ARP 命令

| 分类 | 选项 | 用途 |
| ---- | ---- | ---- |
| arp | -a/-g | 显示当前 ARP 缓存表 |
| arp | -s | 静态绑定 ARP<br>arp -s 10.1.1.1 00-aa-00-62-c 6-09 |
| arp | -d | 删除某条 ARP 缓存<br>arp -d 10.1.10.118 |

# netstat
| 分类 | 选项 | 用途 |
| ---- | ---- | ---- |
| netstat | 无选项 | 显示 TCP 连接，侦听的端口及统计信息 |
| netstat | -a | 显示所有连接和监听端口。 |
| netstat | -n | 以数字形式显示地址和端口号。 |
| netstat | -p | 显示使用特定协议的连接。 netstat -p tcp |
| netstat | -t | 显示 TCP 连接 |
| netstat | -i | 显示网络接口的信息。 |
| netstat | -r | 显示 IP 路由表，效果与 route print 一样 |
| netstat | -atn | 显示所有 TCP 连接并以数字形式显示地址和端口号 |


