# 两个服务两个端口五个报文
- 应用层协议，通过 udp 承载，端口 ==161 和 162==
- 不可靠，效率高，网络管理不会增加太多负载
![[Screenshot_2026-09-22-13-57-13-53_769977972775e0c6b41aa3dfaf766445.jpg]]
udp：500 ipsec 中 ike 做密钥协商会用到
# SNMP 协议操作
- 简化书写，get、get－next 和 set

| 操作编号 | 分类             | 名称               | 用途               |
| ---- | -------------- | ---------------- | ---------------- |
| 0    | 网管找客户端（领导找下属）  | get-request      | 查询一个或多个变量的值      |
| 1    | 网管找客户端（领导找下属）  | get-next-request | 在 MIB 树上检索下一个变量  |
| 2    | 网管找客户端（领导找下属）  | set-request      | 对一个或多个变量的值进行设置   |
| 3    | 客户端反馈（下属向领导汇报） | get-response     | 对 get/set 报文做出响应 |
| 4    | 客户端反馈（下属向领导汇报） | trap             | 向管理进程报告代理发生的事件   |
# 版本
> SNMPv 1：基于团体名认证（密码），==安全性较差==，且返回错误码也较少
> SNMPv 2 c：引入了 ==getbulk==和==inform==操作，支持更多标准错误码，支持更多==数据类型==
> SNMPv 3：在前两个版本基础上重新定义了==网络管框架和安全机制==，提供了基于 USM 的==加密认证==和基于 VACM 的==访问控制==

# v 1 机制与问题
- 管理站与代理站之间可以是==一对多关系，==也可以是==多对一关系==
- SNMP 的认证和控制机制，通过==团体名==验证实现
- 团体名 community==明文传输，不安全